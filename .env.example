# Supabase project credentials (Project Settings → API).
# If left unset, the app falls back to a localStorage-backed data store so
# the full booking flow still works in local development.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
